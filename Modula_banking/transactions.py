#def transfer_to(sender_iban, receiver_iban, transfer_money, database):
    
    #if receiver_iban == database[receiver_iban]['iban']:
            #database[sender_iban]['balance'] -= transfer_money
            #database[receiver_iban]['balance'] += transfer_money
            #return True
    #return None

   